import React from 'react';
import { Paper, Box } from '@mui/material';

const ExpenseGraph = () => {
  return (
    <Paper elevation={2} sx={{ p: 2 }}>
      <Box>מפת הוצאות שנתית (Graph Placeholder)</Box>
    </Paper>
  );
};

export default ExpenseGraph;
